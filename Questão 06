#include <stdio.h>
#include <string.h>

#define MAX 100

int main()
  {
  int tam, i;
  char s1[MAX];
  puts("Digite a frase");
  fgets(s1, MAX, stdin);
  tam = strlen(s1);
  for(i = tam; i >= 0; i--)
    {     
    printf("%c", s1[i]);
    }
  return 0;
  }
  
